title: Pet type
description: A pet type.
allOf:
  - $ref: 'PetTypeFields.yaml'
  - type: object
    properties:
      id:
        title: ID
        description: The ID of the pet type.
        type: integer
        format: int32
        minimum: 0
        example: 1
        readOnly: true
    required:
      - id